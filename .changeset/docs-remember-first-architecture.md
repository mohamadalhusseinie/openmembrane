---
"openmembrane": patch
---

Update all documentation to reflect remember-first architecture. The `remember` tool is now presented as the primary interface for saving memory (no API key needed), with `propose_memory_from_session` documented as the secondary automation path.
